---
interface Props {
    text: string;
    href: string;
    style: "primary" | "selected";
}

const {
    text,
    href,
    style,
    ...rest
} = Astro.props as Props;

import "../styles/custom.css"
---

<a href={href} class:list={[style]} {...rest}>
    <span>{text}</span>
</a>